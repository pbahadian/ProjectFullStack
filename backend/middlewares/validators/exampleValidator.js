//validators de express para una tabla en concreto. Los validators se ejecutaran antes de los controllers cuando recibamos una peticion x a una ruta concreta

// es donde usabamos NEXT -> cuando pasa a la siguiente funcion